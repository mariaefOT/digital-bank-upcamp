import CheckingCards from "../components/CheckingCards";

const ViewChecking = () => {
    return (
        <div>
            <h1 style={{ backgroundColor: 'white', marginBottom: '20px', fontSize: 'calc(19px + 2vmin)'}}>View Checking Accounts</h1>
            <CheckingCards/>
        </div>
    )
}

export default ViewChecking;